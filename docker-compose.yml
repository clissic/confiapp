services:
  mongo:
    image: mongo:7
    container_name: confiapp-mongo
    restart: unless-stopped
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_DATABASE: confiapp
    volumes:
      - confiapp_mongo_data:/data/db

volumes:
  confiapp_mongo_data:
