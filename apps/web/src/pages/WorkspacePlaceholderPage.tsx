/** Placeholder de contenido dentro del layout — no es una pantalla de producto. */
export function WorkspacePlaceholderPage() {
  return (
    <section>
      <p className="ca-page__kicker">ConfiApp</p>
      <h2 className="ca-page__title">Layout principal</h2>
      <p className="ca-page__lead">
        Estructura visual lista: sidebar, topbar, breadcrumbs, menú de usuario, responsive, modo
        oscuro y animaciones. Sin funcionalidades de negocio.
      </p>
    </section>
  );
}
