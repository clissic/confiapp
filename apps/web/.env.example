# API (build de producción: URL absoluta del backend)
# En desarrollo (`pnpm dev`) el front usa same-origin + proxy Vite automáticamente
# (Local, Network/IP LAN y túneles) — no hace falta cambiar la IP.
VITE_API_URL=http://localhost:3000

# App
VITE_APP_NAME=ConfiApp
VITE_APP_ENV=development
